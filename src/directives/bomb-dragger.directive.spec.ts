import { BombDraggerDirective } from './bomb-dragger.directive';

describe('BombDraggerDirective', () => {
  it('should create an instance', () => {
    const directive = new BombDraggerDirective();
    expect(directive).toBeTruthy();
  });
});
